There are multiple experiments in this chapter, each in its
directory: cell, circles, images, store.

The 'include' directory contains the SI and EA
code.  The Python scripts will find it, so run them from
their respective directories.

